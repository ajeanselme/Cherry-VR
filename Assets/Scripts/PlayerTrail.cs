using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

public class PlayerTrail : MonoBehaviour
{

   /* private float timerShadows;
    public float startTimerShadows;

    public GameObject shadow;
    public InputActionProperty moveInput;



    void Update()
    {
        if (moveInput.action.ReadValue<Vector2>().x != 0)
        {
            if (timerShadows >= 0.05f)
            {
                GameObject instance = (GameObject)Instantiate(shadow, transform.position, Quaternion.identity);
                Destroy(instance, 0.1f);
                timerShadows = startTimerShadows;
            }
            else
                timerShadows += Time.deltaTime;
        }
    }*/
}
